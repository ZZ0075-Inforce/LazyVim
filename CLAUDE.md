# CLAUDE.md

個人 Neovim（LazyVim）設定 repo。慣例：直接 commit main，歷史為 checkpoint 式（hook 自動前置表頭）。

## Agent skills

### Issue tracker

Issues 追蹤於本 repo 的 GitHub Issues（`gh` CLI）。See `docs/agents/issue-tracker.md`.

### Triage labels

採用五個標準 triage 標籤，名稱即字串（`needs-triage` 等）。See `docs/agents/triage-labels.md`.

### Domain docs

Single-context：根目錄 `CONTEXT.md` + `docs/adr/`（由 `/domain-modeling` 惰性建立）。See `docs/agents/domain.md`.
